# Active Context

## ЗАДАЧА В РАБОТЕ: нет (активных задач нет)

## Выполнено 23.08.2026: третий кейс «Мир Кунфу»
**Отчёт/план: `Доп/отчёт_кейс3_МирКунфу_планИнтеграции.md` — чек-лист выполнен по всем 10 пунктам.**
- Перенесён ТОЛЬКО компонент `components/case-study/kunfu/case-study-page.tsx` (питч-дек и слайды НЕ переносились — см. решение владельца).
- Кнопка-ссылка «Смотреть результат» в шапке кейса → **https://pitch-mauve-omega.vercel.app/** (опубликованный 12-слайдовый питч-дек; ссылку прислал владелец 23.08.2026).
- Back-link «← Все проекты» → `/#cases` в шапке `case-nav` (добавлен по просьбе владельца после деплоя, как на scenario/titanic): стоит по центру вместо декоративного «CASE STUDY / 2026», стили `.case-nav-back` (красная стрелка-акцент, hover → paper); на экранах ≤800px скрыт — мобильная шапка остаётся компактной (логотип + CTA).
- Маршрут `app/kunfu/page.tsx` (обёртка `.theme-kunfu`) + `app/kunfu/layout.tsx` с метаданными — по образцу scenario/titanic.
- Шрифты (корневой `app/layout.tsx`): `Geist` → `--font-body`, `Noto_Serif_SC` → `--font-cjk`, `Fraunces` italic (latin+cyrillic-ext, 400/500/600) → `--font-display-kunfu`; навешены на `<html>`, файлы шрифтов скачиваются только там, где реально используются.
- globals.css: в не-инлайновый `@theme` добавлен `--font-display: var(--font-display-kunfu)`; блок `.theme-kunfu` с тёмными чернильно-киноварными токенами (oklch из исходного `:root` кейса + `--font-sans: var(--font-body)`, `--font-display: var(--font-display-kunfu)`); кастомный CSS кейса (`.case-page … .bg-grain`, строки 95–154 исходного globals.css) перенесён дословно в конец файла — селекторы изолированы префиксами `.case-`/`.hero-`/`.mini-deck`, конфликтов нет (проверено grep-ом до переноса).
- cases.tsx: «Кейс 03» — больше не заглушка, живая карточка → `/kunfu` (tag «Медиа-экосистема / Питч-дек», CTA «Читать полный кейс», тексты по черновику из плана). Механизм `locked` в шаблоне сохранён для будущих кейсов.
- Превью карточки: нарисован SVG `public/cases/kunfu-case.svg` в палитре кейса (ink #171412, cinnabar #A54535, paper #EEE7DA, иероглифы 功夫/功).
- Проверка: `pnpm build` exit 0 → 6 статических страниц; локальный прогон `out/` — `/`, `/kunfu/`, `/scenario/`, `/titanic/`, `/cases/kunfu-case.svg` все 200; внешняя ссылка на питч-дек присутствует в HTML страницы кейса.

## Выполнено 23.08.2026 (второй проход): перестановка карточек 02/03 по яркости
- Фидбек владельца: яркостной дисбаланс — две светлые карточки слева и одна тёмная справа.
- `cases.tsx`: порядок в сетке теперь **Кейс 01 «Сценарий» (светлая) → Кейс 02 «Мир кунфу» (тёмная, центр) → Кейс 03 «Titanic English» (светлая)** — чередование света/тьмы.
- Перенумерация синхронизирована: `cases.tsx` (index-поле), комментарии в `app/globals.css` (4 места) и `app/layout.tsx`, `README.md` (таблица + структура + актуальный URL och-studio.ru), `productContext.md`, `projectbrief.md`.
- Маршруты `/kunfu`, `/titanic` и все URL НЕ менялись — только номера и порядок. Reveal-задержки карточек следуют новому порядку автоматически.

## Выполнено 23.08.2026 (третий проход): кнопки-CTA на карточках кейсов
- По фидбеку владельца: ссылку «Читать полный кейс» превратили в кнопку и усилили визуально.
- `cases.tsx`: CTA карточек — Base UI `Button` с `render={<Link/>}` + `nativeButton={false}` (идиома кнопки «Смотреть кейсы» из hero).
- Стиль: `rounded-full bg-primary` (шампань), лёгкий подъём `hover:-translate-y-0.5`, свечение `shadow-primary/20 → hover:shadow-primary/40`, иконка `ArrowUpRight` сохранена.
- Текст: «Читать полный кейс» → **«Смотреть полный кейс»** (во всех трёх карточках). Вариант владельца «Смотреть!» сознательно не взят — полная фраза понятнее в контексте портфолио; при желании сокращается одной правкой строк `cta` в `CASES`.

## Выполнено 23.08.2026 (правка страницы `/kunfu`): вертикальный ритм заголовков h2
- Фидбек владельца: на «слайдах» 01/03/04 (Обзор / Стратегия / Система) буквы белой и красной строки h2 «прилипают».
- **Диагноз:** общее правило `.case-section h2 { line-height: .87 }` (перенесено из исходника) при размере заголовка до 7rem давало зазор между базлайном белой строки и капами красной всего ~0.12em (~13px). В 01/03/04 красная строка начинается С ЗАГЛАВНЫХ («Медиа-экосистема.», «Пять доказательств.», «Живой акцент.») → прилипание; в 02/05 красные строки со строчных («нужно…», «готовая…») → визуально ок, поэтому владельцем и не названы.
- **Фикс:** оверрайд в конце globals.css — `.case-section h2 { line-height: 1.02; }` (капам ~0.27em воздуха, единый ритм всех секций). Hero `h1` не тронут (своё сцепление .87).
- Проверка: `pnpm build` exit 0; правило присутствует в собранном CSS `/kunfu`.

## Выполнено 23.08.2026 (хидер `/kunfu`): центровка логотипа + акценты действий
- Фидбек владельца: ярлык «МИР КУНФУ» — по центру, «Все проекты» — слева (единообразие с другими кейсами), обе ссылки терялись на фоне.
- Хидер перестроен: `case-nav` теперь grid `1fr auto 1fr` → слева back-link, истинный центр — badge MK, справа CTA. Порядок детей в TSX: back → logo → CTA.
- Back-link: цвет paper 100% (не 55%), красная стрелка `.case-back-arrow`, hover → red. На мобильных ≤800 остаётся стрелка (метка `.case-back-label` скрыта) — как в шапках scenario/titanic. **Отменяет** прежнее решение «скрывать back-link на мобильных».
- CTA «Смотреть результат»: залит киноварью (`background: var(--case-red)`, paper-текст, красная рамка = чип), hover — инверсия в красный контур/красный текст (transition .25s). Класс `case-nav-link` (приглушённый 55%) снят с обеих ссылок.
- **Нюанс сборки:** lightningcss (Turbopack) меняет порядок деклараций и минифицирует — проверять правила в собранном CSS по вхождениям селектора и смещениям, а не по буквальному совпадению строки (`display:grid` нашёлся внутри `.case-nav{grid-template-columns:…;display:grid}` на позиции ПОСЛЕ базового flex-правила).

## Выполнено 23.08.2026 (hero `/kunfu`): воздух между h1 и подзаголовком
- Заметка владельца: lede «Как превратить стратегию…» визуально прилипал к красной строке «кунфу».
- Причина: `h1 line-height: .87` (плотный lock-up) поднимает низ line-box последней строки над базлайном (~−13px на 13rem), и `margin-top: 32px` lede визуально съедался до ~15–20px (плюс десендеры «у/ф»).
- Фикс (оверрайд в конце globals.css): `.hero-lede { margin: 64px 0 0 32%; }` + повторное мобильное правило `@media (max-width:800px) { .hero-lede { margin: 44px 0 0 0; } }` — стоит ПОСЛЕ исходного медиа-блока, поэтому каскад гасит десктопный отступ 32% и на мобильных.
- Проверка позиций в собранном CSS: 64px-правило после 32px-правила, мобильное 44px-правило после `margin-left:0`.

## Выполнено 23.08.2026 (hero `/kunfu`): текст lede и трекинг h1
- По заметке владельца: в lede порядок после запятой — «…в историю, **частью которой хочется стать.**» (было «которой хочется стать частью»).
- Межбуквенный интервал hero-заголовка увеличен: общий `letter-spacing: -.06em` (наследуется от правила `.hero-content h1, .case-section h2`) давал слипание засечек Fraunces на гигантском кегле; оверрайд в конце globals.css — `.hero-content h1 { letter-spacing: -0.03em; }` — чуть ослабляет, сохраняя плотность lock-up. Заголовки секций h2 остаются на -.06em (замечаний по ним не было).

## Выполнено 23.08.2026 (`/kunfu`): выравнивание номеров секций с ярлыками
- Заметка владельца: номера «01…» и названия секций («Проект в двух словах» и др.) не выровнены.
- Причина: ярлыки (SectionLabel = `text-xs` → 12px/16px LH, первый элемент `section-main`) и номера (Fraunces 24px, без line-height → `normal` ≈ 1.2–1.3em из-за высоких асцендеров Fraunces) стартуют с одного верха grid-ячеек, но полуведущий интервал + крупный ascent сажали цифры на ~3–4px ниже капов ярлыка.
- Фикс: `.section-number { line-height: 1; }` оверрайдом в конце globals.css (позиция ПОСЛЕ базового правила и медиа-блока). Паттерн одинаков во всех 5 секциях — одно правило; на мобильных (block-раскладка) цифры встают вплотную к ярлыку тоже.

## Выполнено 23.08.2026 (РЕДИЗАЙН страницы `/kunfu`): вариант А — светлая «галерея/архив»
- Запрос владельца: дизайн кейса был практически идентичен дизайну самой презентации (одна палитра ink+cinnabar, один Fraunces, одна композиция) → пользователь путал «разбор работы» с «работой». Вариант А согласован.
- Токены `.theme-kunfu` переписаны на светлую тему: бумага `#f3ede0`, тушь `#211a11`, primary = киноварь `#a54535`, body = Inter, display = **Newsreader** (нативный italic + кириллица).
- В конец globals.css добавлен SKIN-слой (~70 строк, ПОСЛЕ всех прежних оверрайдов): роли `--case-ink`/`--case-paper` инвертированы (поверхность ↔ текст), все захардкоженные paper-альфа линии заменены ink-линиями, мета/ярлыки/рейлы — Plex Mono, заголовки и цитаты — Newsreader, CTA шапки — чернильный чип (hover → контур).
- **Артефакты deck сохранены тёмными** (палитра свотчей INK/CINNABAR/PAPER + мини-деки с родным Fraunces `--font-display-kunfu`) — на светлом фоне они читаются как экспонаты продукта внутри кейса.
- Красный дизайн hero-преложен мягче (радиал 20% → 10%).
- Превью карточки перерисовано в галерейный вид: `public/cases/kunfu-case.svg` — бумага, чернильный lock-up, тёмный мини-дек как экспонат справа.
- Документация синхронизирована: README (таблица тем), productContext, projectbrief.
- Прошлые точечные правки кунфу-страницы (line-height h2 = 1.02, hero-lede отступы, трекинг h1, grid-шапка, выравнивание номеров) СОХРАНЕНЫ — скин ложится поверх них.
- Проверка: build exit 0; в собранном CSS скин последний по каскаду; локально `/kunfu/` → 200, тема светлая применяется.

## Выполнено 23.08.2026 («Финансовая модель» — новый экран секции 05 на `/kunfu`)
- Запрос владельца: в презентации на 10-м слайде есть ветка по кнопке «Финансовая модель» — отразить в кейсе отдельным экраном, что для инвесторов разработана финансовая составляющая (доходность + окупаемость).
- Бранч исследован в живом бандле deck'а (chunk `1tvbytz1pj0kn.js`): 11 суб-слайдов (`fin-assumptions … fin-risks`): Допущения → Юнит-экономика → Маржа тура → Расходы → Сметы → Доходы (8 потоков) → P&L → Безубыточность → Инвестору → Сценарии/кэш-флоу → Риски и резюме.
- Реальные цифры ветки, использованные в кейсе: полная окупаемость — **Год 8** (базовый сценарий), операционная безубыточность Годы 4–5, чистая маржа зрелой фазы **15–25%**, накопленная прибыль к 10-му году **~15 млн ₽**, десятилетний результат по сценариям: пессимистичный −6 / базовый +2 / амбициозный +9 (млн ₽), потребность в капитале ≈ 11,65 млн ₽ (~$125 тыс), горизонт 10 лет.
- Маркап: секция `#finance` (№ 05) — label «Финансовая модель», h2 «Инвестору нужны *цифры.*», large-copy + facts-list (формат/вход/состав/горизонт), затем тёмные артефакты-плитки метрик и тёмный ledger сценариев. «Результат» перенумерован 05 → **06**, в его copy добавлено «с отдельной финансовой веткой». В массив `sections` добавлен `['finance','Финансы']` (рельф и observer автоматом).
- CSS: `.finance-metrics`/`.finance-metric`/`.finance-scenarios`/`.scenario(.is-base)` — тёмный язык deck'а на светлой галерее (Fraunces цифры через `--font-display-kunfu`, палитра #211b18/#EEE7DA/#A54535, mono-подписи); мобильная адаптация (1 колонка).

## Выполнено 23.08.2026 (CTA и подпись на картинке — `/kunfu`)
- По запросу владельца (аналогично кейсам 01/03 с их «Обсудить проект»): на ПЕРВОМ и ПОСЛЕДНЕМ экранах кейса добавлены коммерческие кнопки → `/#contact`:
  - hero: после lede — пара «Заказать презентацию» (solid: чернильный чип, инверсия на hover) + «Обсудить идею» (ghost: контур, red-стрелка) — классы `.hero-cta`, `.case-cta-solid`, `.case-cta-ghost`;
  - outcome (06): новый `.cta-band` после outcome-grid — серифный заголовок «Нужна своя *история для инвесторов?*» + та же пара кнопок (`.cta-band-actions`), над футером.
- Превью-картинка `public/cases/kunfu-case.svg` получила подпись по аналогии с другими кейсами: курсивная серифная строка **«От идеи проекта — к инвестиционному предложению»** под lock-up'ом; композиция перебалансирована (功夫 ниже, caption теперь «БУМАГА · ТУШЬ · КИНОВАРЬ · ФИНАНСОВАЯ МОДЕЛЬ»).

## Текущий статус
Проект объединён, собран (`pnpm build` — exit 0), закоммичен в git и запушен в репозиторий `bars4um-sys/oleg-portfolio`. Настроен автодеплой через GitHub Actions на GitHub Pages.

## Что сделано
- База портфолио в корень репозитория.
- Компоненты кейсов неймспейсы: `components/case-study/scenario/` и `.../titanic/` (избежали коллизий reveal/hero/section-label/cta-footer/brief-section).
- Импорты внутри кейсов переведены на неймспейсы (sed).
- Маршруты `app/scenario` и `app/titanic` с theme-обёртками.
- Шрифты (Cormorant Garamond, Inter, Fraunces, Newsreader, IBM Plex Mono) загружены в root layout.
- globals.css: токены кейсов вынесены в `.theme-scenario` / `.theme-titanic`; шрифты — в non-inline `@theme` (переопределяются per-theme).
- cases.tsx: ссылки на `/scenario` и `/titanic` через next/link.
- Хидеры кейсов: «назад» на `/`.
- next.config: `output: 'export'`, `trailingSlash: true`, basePath из `NEXT_PUBLIC_BASE_PATH`.
- Workflow `.github/workflows/deploy.yml`: автоопределение basePath по имени репо.
- pnpm-workspace.yaml: `dangerouslyAllowAllBuilds: true`, `verifyDepsBeforeRun: false` (обход `ERR_PNPM_IGNORED_BUILDS` для транзитного `msw`).
- **Важно (фото-фикс):** `next/image` и обычные `<img>` НЕ применяют basePath автоматически при static export — добавлен helper `asset()` в `lib/utils.ts` (префиксит `process.env.NEXT_PUBLIC_BASE_PATH`) и применён ко всем src картинок (cases, hero, author, scenario/hero, scenario/script-frame, titanic/hero, titanic/browser-frame). Также префикс проставлен для favicon в `app/layout.tsx` (metadata.icons). Без этого все картинки 404-ились на проектном Pages-сайте.

## Контакты (правки)
- Telegram-кнопка в футере (`Написать в Telegram`) → `https://t.me/OlegUmaPalata` (рабочая).
- Hero: убрана кнопка «Написать в Telegram» (осталась только «Смотреть кейсы»).
- Header «Обсудить проект» → `#contact` (раздел «Контакты»).
- Header nav: добавлен пункт «Контакты» → `#contact`.
- Footer: «Посмотреть контакты» → «Написать в ВК» (icon MessageCircle); добавлена «Написать в MAX» (icon MessageSquare). Ссылки реальные: ВК `https://vk.me/oleg4um`, MAX `https://max.ru/u/f9LH...`.

## Страница кейса scenario (правки)
- Header: убрана «застревающая» надпись «СЦ. 01 / Кейс-стади · Портфолио». Вместо неё — back-link «← Все проекты» → `/#cases` (в стиле рукописи: font-mono, hover:text-redline, иконка ArrowLeft). Правый CTA — «Обсудить проект» → `/#contact`.
- Нумерация СЦ начинается со 2-го блока (hero = «INT.» без номера): brief=СЦ.01, process=СЦ.02, program=СЦ.03, author=СЦ.04, result=СЦ.05, metrics=СЦ.06 (раньше было 02..07).
- Блок «Автор» (СЦ.04): SectionLabel вынесен НАД двухколоночной сеткой (выровнен как в других блоках), grid получил `mt-8 md:mt-12`, у h2 убран `mt-4`.

## Последующие правки
- CTA на страницах кейсов (scenario + titanic): кнопка шапки и cta-footer → «Обсудить проект» → `/#contact` (блок контактов главной). Внешняя «Смотреть готовый сайт» (course.kinomagica.ru) у сценария осталась.
- Блок «Автор» (главная): удалены недостоверные регалии (8+/40+/×2.8) → декоративное трио иконок Clapperboard · Package · PenTool (без подписей/цифр).
- Hero: нейро-сетка обрезана на левые ~2/3 (`neuroRegion="top-0 bottom-0 left-0 right-1/3"`), чтобы не перекрывать портрет справа; золотые блобы — на весь блок.
- Карточки кейсов: контейнер `aspect-[16/10]` (совпадает с нативным соотношением скриншотов «сценария» 1440×900 / 1231×754).
- **Оптимизация изображений:** весь используемый растр → WebP (`cwebp -q 82`, ресайз под retina: скриншоты/фон 1280px, карточки 900px, premium 800px, мобильный без апскейла). Удалены 4 неиспользуемых PNG (~6.4МБ). `public/` ≈14.5МБ → ≈1МБ. Ссылки в коде обновлены (`.png`/`.jpg` → `.webp`), basePath через `asset()` сохранён.

## Статус
- **Сайт живой:** https://bars4um-sys.github.io/oleg-portfolio/ (репо: `bars4um-sys/oleg-portfolio`).
- Все 3 страницы рендерятся; картинки и favicon загружаются (HTTP 200); nav и CTA обновлены.
- Контакты (Telegram/ВК/MAX) — все ссылки реальные и рабочие.

## Инцидент 16.08.2026: «сайт не работает с гитхаба»
- Симптом у пользователя: деплой-воркфлоу падал на шаге `actions/configure-pages` с
  `Get Pages site failed. Please verify that the repository has Pages enabled...`.
- Причина: объект GitHub Pages у репо был в неактивном состоянии (`GET /pages` → 404,
  у PAT-запроса `status: null`), хотя старый артефакт ещё отдавался (сайт «вроде живой»,
  но деплой сломан). Лечится самим деплоем: ручной `workflow_dispatch` реактивировал Pages.
- **Исправление (коммит `1781ef5`, pushed):** в `.github/workflows/deploy.yml`
  - `actions/configure-pages@v5` → `@v6` (Node 24, убирает deprecation-warning);
  - добавлены `token: ${{ secrets.PAGES_PAT || github.token }}` и `enablement: true` —
    авто-включение Pages, если запись сайта пропала/неактивна (это и советует сам error-месседж).
  - Опционально: чтобы enablement работал на 100%, можно добавить PAT (scope `repo` или
    fine-grained `Pages: write`) в repo secrets под именем `PAGES_PAT`.
- Примечание: warning про «Node.js 20 is deprecated» для checkout/setup-node/pnpm — некритичен.

## Копирайт-ревизия 17.08.2026 (по фидбеку реальных заказчиков)
Фидбек: макет дорогой, но текст на «дизайнерском птичьем языке», местами снисходительный. Внедрено:
- Header: «ОЛЕГ ЧУМАЧЕНКО / Веб-дизайнер & Продуктовый верстальщик» → **«OCH STUDIO / Олег Чумаченко — Веб-дизайн и упаковка экспертных продуктов»**.
- Hero: оверлайн «...& РЕДИЗАЙН ЛЕНДИНГОВ» → «...И РЕДИЗАЙН САЙТОВ»; H1 «Превращаю сухие программы и тексты в лендинги...» → **«Превращаю сложные материалы и программы в сайты, которые дочитывают до конца и покупают»**; подзаголовок переписан в бизнес-терминах (цифры, ясная логика, лёгкое восприятие).
- «Почему это работает»: карточки «Смысловой аудит / Эстетика как инструмент / Адаптивная верстка» → **«Фокус внимания за 3 секунды / Визуальное доверие / Логика вместо хаоса»**.
- Услуги: & → и (заголовки и тексты), «...под ключ» без кавычек, С1/С2 тексты переписаны по-человечески.
- Блок «Об авторе»: убран «продуктовый верстальщик» и «а не как реферат»; alt портрета синхронизирован.
- Footer/CTA: «Готовы переупаковать...» → **«Хотите оценить потенциал вашего сайта или презентации?»**, мягкий текст, кнопка «Написать в Telegram»; копирайт © — «Веб-дизайн и упаковка экспертных продуктов».
- Кейс «Титаник»: запятая «История о том, как...»; «Курс построен на живой идее.../синхронный перевод» → «Идея курса — изучение живого английского через просмотр фильма... и синхронный разбор диалогов».
- app/layout.tsx: <title>, description, OG/Twitter и alt превью приведены к новой формулировке.
- Глобально: во всех видимых текстах & → «и» (оставлен только в бренде OCH STUDIO).

## Домен och-studio.ru (17.08.2026)
- Кастомному домену подключён к репозиторию через API (`cname=och-studio.ru`, PUT /pages).
- DNS в Timeweb переведён с 188.225.23.140 на GitHub Pages: `@` → 4×A 185.199.108–111.153, `www` → CNAME bars4um-sys.github.io. MX/TXT(SPF)/NS оставлены как есть (почта на Timeweb).
- `public/CNAME` = `och-studio.ru` закоммичен (попадает в `out/`).
- `bars4um-sys.github.io/oleg-portfolio/` теперь 301 → `och-studio.ru`.
- Статус: **HTTPS активен.** GitHub выпустил сертификат (Let's Encrypt) для `och-studio.ru`; `html_url=https://och-studio.ru/`, `https_enforced=true` (закреплён через PUT). `http://och-studio.ru/` → 301 → https; `www` (CNAME) → 301 → apex. Все страницы/ассеты 200 по https.
- Важный блокер, из‑за которого тупил HTTPS: в DNS оставалась **старая IPv6 (AAAA) Timeweb `2a03:6f00:6:1::bce1:178c`** — GitHub пишет «DNS check successful», но не выпускает сертификат, пока не убрать AAAA старого хостинга. Починили: AAAA удалена (авторитетные NS Timeweb пусты), IPv4 = 4× официальных A GitHub Pages (185.199.108–111.153).

## Инцидент 18.08.2026: «чёрный экран на och-studio.ru»
- **Симптом:** сайт не загружался — чёрный экран, CSS и JS не подгружались (404 на `/oleg-portfolio/_next/static/chunks/3__hlwd6vdur_.css` и других ассетах).
- **Корень проблемы:** workflow `deploy.yml` вычислял `NEXT_PUBLIC_BASE_PATH=/oleg-portfolio` по имени репозитория (project Pages repo). Но кастомный домен `och-studio.ru` обслуживается GitHub Pages из корня (`/`), без префикса `/oleg-portfolio`. HTML получался с путями `/oleg-portfolio/_next/...` и `/oleg-portfolio/cases/...`, а файлы лежали на диске Pages по пути `/_next/...` и `/cases/...` (корень).
- **Исправление (commit `1c3a25c`, pushed):** в `.github/workflows/deploy.yml` добавлена проверка `if [[ -f public/CNAME ]]` — если CNAME файл есть (кастомный домен), `NEXT_PUBLIC_BASE_PATH` устанавливается в пустую строку. Это заставляет Next.js генерировать пути к ассетам без префикса, как требуется для корневого сервинга на кастомном домене.
- **Требуется:** `workflow_dispatch` для принудительного пере-деплоя на och-studio.ru (push уже отправлен, но GitHub Pages может кэшировать старый артефакт).
