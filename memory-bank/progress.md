# Progress

## В работе
- Нет.

## Работает
- Сборка `pnpm build` → exit 0, генерация `out/` с маршрутами `/`, `/scenario`, `/titanic`, `/kunfu`.
- Темы кейсов применены (theme-scenario/theme-titanic в HTML).
- **Ребрендинг главной (19.09.2026, коммит `8297839`):** графитово-контрастная тёмная тема `#12110f` + карточки `#29231e` + яркий янтарный акцент `#ffb548`/hover `#ffd078`; светлая секция услуг `.theme-services` (`#f2e8d8`); финальный CTA — янтарная панель.
- **Полировка главной (19.09.2026, коммит `3c9f207`):** ослаблено свечение CTA, плашка услуг почти белая (`#faf3e7`) с деликатным разделителем, мелкий текст шапки `#D2C8BA`, ускорено появление hero (описание 160мс / кнопка 200мс).
- **Стартовая анимация hero (19.09.2026, коммит `7325380`):** полный показ за 0.9с (задержки 0/100/250/400мс, длительность 500мс) через новый проп `duration` у `Reveal`; остальные секции сохраняют 0.8с.
- **Оживление фона и интерактива главной (19.09.2026):** медленное янтарное световое пятно по секциям, локализованная сетка с импульсами, глобальное плёночное зерно, деликатный hover карточек, дыхание CTA, поддержка `prefers-reduced-motion` и мобильная адаптация.
- Навигация: главная → концепции/кейсы (`/scenario/`, `/titanic/`, `/kunfu/`), обратно → главная (`/`).
- Ассеты (изображения, favicon) на месте.
- Все изображения оптимизированы в WebP.

## Известные особенности
- `Доп/` (вспомогательные материалы/отчёты) закоммичен — можно вынести в .gitignore при желании.
- Кейс 02 «Мир кунфу» — живой: карточка на главной → `/kunfu/`, кнопка «Смотреть результат» → внешний питч-дек.
- **«Титаник» (Кейс 03) перепозиционирован 29.08.2026:** из «кейса с метриками» в «инициативную концепцию». Убраны все количественные показатели (×2.8, −46%, +64%, +92%), смягчена критика исходного сайта, добавлены дисклеймеры о статусе проекта. Детали: `Changing.md`.
- Контакты: Telegram (`@OlegUmaPalata`), ВК (`vk.me/oleg4um`), MAX (`max.ru/u/f9LH...`) — все ссылки реальные и рабочие.

## Эволюция решений
- Сначала пытался `onlyBuiltDependencies` в pnpm-workspace.yaml — не сработало (старое имя ключа в pnpm 11; нужно `allowBuilds`/`dangerouslyAllowAllBuilds`).
- Гипотеза про `CI=true` отвергнута (CI=0 не помогло); причина — pnpm `verifyDepsBeforeRun` + игнор build-скриптов.
- Изображения: PNG/JPG → WebP через `cwebp -q 82 -resize <w> 0`, ресайз только если исходник шире цели (без апскейла); неиспользуемые файлы удалялись.
- Нумерация СЦ сценария: единый проход regex (`СЦ. N` → `N-1`), затем подпадывание двух разрядов — чтобы избежать двойного декремента при последовательных заменах.
- **basePath для кастомного домена:** в workflow добавлена проверка `public/CNAME` — если CNAME есть, `NEXT_PUBLIC_BASE_PATH` пуст (корень), иначе используется логика по имени репо. После фикса все ассеты ссылаются на корень домена. Коммит `1c3a25c`.
